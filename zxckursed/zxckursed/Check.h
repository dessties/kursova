#pragma once
#include <fstream>
#include <iostream>
#include <string>

using namespace std;

class Check
{
public:
	bool CheckData(ofstream file, string username)
	{
		while (!file.eof())
		{
		}
	}
};

